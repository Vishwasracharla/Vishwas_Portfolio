'use client';

const BlobAnimation = () => {
  return (
    <div className="fixed inset-0 -z-10 h-full w-full overflow-hidden bg-[#2A2A2A] opacity-30 blur-3xl">
      {/* Minimal blob */}
    </div>
  );
};

export default BlobAnimation; 